function suma(a){ // El valor dentro de los parentesis se conoce como el parametro
    return a + 2
}

let resultado = suma(5); // Los argumentos son los valoress que se pasan como parametros
console.log(resultado);